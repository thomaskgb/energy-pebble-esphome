# WiFi credentials
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Security
ota_password: "YOUR_OTA_PASSWORD"
# Note: Fallback AP is now open (no password) for easy initial configuration